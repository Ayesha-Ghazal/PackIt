apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: packit-ingress

spec:
  ingressClassName: nginx

  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: packit-backend-service
                port:
                  number: 5000